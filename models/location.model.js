class Location {
    constructor(
        longitude,
        latitude,
        name,
        address
    ) {
        this.longitude = longitude
        this.latitude = latitude
        this.name = name
        this.address = address
    }
}

module.exports = Location